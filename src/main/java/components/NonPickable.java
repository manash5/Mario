package components;

// this class is made only so that gizmos cannot be called as active object when clicked
// we didn't made gizmos an active object because when it is, it just flies away since it is constantly rendering itself
public class NonPickable extends Component{
}
