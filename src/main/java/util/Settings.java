package util;
// stores the size of the grid which is the boxes in the level editor scene

public class Settings {
    public static float GRID_WIDTH = 0.25f;
    public static float GRID_HEIGHT = 0.25f;
}
